@echo off
REM Copy to .env.bat and fill real values before starting the backend.

set "SMTP_USER=your.email@gmail.com"
set "SMTP_APP_PASSWORD=your_gmail_app_password"
set "SMTP_FROM=%SMTP_USER%"

REM Optional local-dev flags (uncomment if needed).
REM set "OTP_DEBUG=true"
REM set "EMAIL_DEBUG=true"
