# booking-bus-ticket (Java Yubus Project)

Local standalone copy of the Yubus booking project.

## Run locally

1. Open `Frontend/start-backend.bat` to start the Java backend on `http://localhost:8081`
2. Open `Frontend/index.html` in the browser
